% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical-decision.R
\name{clinicalDecision}
\alias{clinicalDecision}
\title{Run the clinical decision loop end-to-end}
\usage{
clinicalDecision(
  imu,
  prior,
  estimate = "strength",
  candidates = defaultInterventions(),
  outcome = "rom",
  method = c("optim", "ukf"),
  dt = 0.005,
  n_mc = 200,
  weights = c(efficacy = 1, certainty = 1, risk = 1, burden = 0.5),
  seed = NULL
)
}
\arguments{
\item{imu}{Patient IMU data (as from \code{\link[=imuMeasure]{imuMeasure()}}).}

\item{prior}{A \code{movement_twin} prior/initial guess for \code{\link[=personalizeTwin]{personalizeTwin()}}.}

\item{estimate}{Parameters to personalise.}

\item{candidates}{Candidate interventions (default \code{\link[=defaultInterventions]{defaultInterventions()}}).}

\item{outcome}{Outcome to optimise.}

\item{method}{Personalisation method (\code{"optim"}/\code{"ukf"}).}

\item{dt, n_mc, weights, seed}{As in \code{\link[=scoreInterventions]{scoreInterventions()}}.}
}
\value{
a \code{clinical_decision}: \code{fit}, \code{readiness}, and \code{scores}.
}
\description{
Personalises a twin to a patient's IMU data, gates it with \code{\link[=twinReadiness]{twinReadiness()}},
and -- only reporting through the gate's confidence -- scores and ranks the
candidate interventions. A one-call demonstration of the loop; the returned
object holds the fit, the readiness verdict and the ranked recommendations.
}
\examples{
\donttest{
truth <- limbTwin(strength = 6, damping = 0.6, stiffness = 3)
imu <- imuMeasure(simulateTwin(truth, 6, 0.01), imuSensor(gyro_noise = 0.03), seed = 1)
dec <- clinicalDecision(imu, limbTwin(strength = 4), estimate = "strength",
                        dt = 0.01, n_mc = 40, seed = 1)
dec$scores$ranking$intervention[1]
}
}
\seealso{
\code{\link[=personalizeTwin]{personalizeTwin()}}, \code{\link[=twinReadiness]{twinReadiness()}}, \code{\link[=scoreInterventions]{scoreInterventions()}}
}
