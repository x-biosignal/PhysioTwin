% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-pgibbs.R
\name{particleGibbs}
\alias{particleGibbs}
\title{Particle Gibbs with ancestor sampling}
\usage{
particleGibbs(
  build_model,
  y,
  prior_logd,
  init,
  n_particles = 100,
  n_iter = 2000,
  proposal_sd = 0.1,
  burn_in = 0.3,
  seed = NULL
)
}
\arguments{
\item{build_model}{\verb{function(theta)} returning the state-space model: a list
\code{f}, \code{h} (\verb{function(x, k)}), \code{Q}, \code{R}, \code{x0}, \code{P0}.}

\item{y}{Observation matrix (\verb{n_steps x m}) or vector.}

\item{prior_logd}{\verb{function(theta)} log prior density (\code{-Inf} off the support).}

\item{init}{Initial parameter vector (its names label the output).}

\item{n_particles}{Particles in the conditional SMC.}

\item{n_iter}{Iterations.}

\item{proposal_sd}{Initial per-parameter proposal SD.}

\item{burn_in}{Fraction discarded (and over which the proposal adapts).}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{pgibbs_result}: parameter posterior \code{samples}, \code{acceptance}, \code{mean},
\code{sd}, 2.5/50/97.5\% \code{quantiles}, and the posterior-mean state \code{trajectory}.
}
\description{
Samples the joint posterior of a state-space model's latent trajectory and
parameters by particle Gibbs: each iteration draws a new state trajectory with a
conditional SMC (ancestor-sampling) sweep given the current parameters, then
moves the parameters with a Metropolis step using the tractable complete-data
likelihood of that trajectory. Ancestor sampling gives good mixing even for long
series where plain particle Gibbs degenerates.
}
\examples{
\donttest{
set.seed(1); a <- 0.8; x <- numeric(50); for (k in 2:50) x[k] <- a * x[k-1] + rnorm(1, 0, 0.3)
y <- matrix(x + rnorm(50, 0, 0.3), 50, 1)
bm <- function(th) list(f = function(x, k) th[1] * x, h = function(x, k) x,
                        Q = matrix(0.09), R = matrix(0.09), x0 = 0, P0 = matrix(1))
fit <- particleGibbs(bm, y, function(th) dunif(th[1], -1, 1, log = TRUE),
                     init = c(a = 0.4), n_particles = 60, n_iter = 600, seed = 1)
fit$mean
}
}
\references{
Andrieu C, Doucet A, Holenstein R (2010) J R Stat Soc B 72:269-342;
Lindsten F, Jordan MI, Schon TB (2014) J Mach Learn Res 15:2145-2184.
}
\seealso{
\code{\link[=particleMCMC]{particleMCMC()}}, \code{\link[=metropolis]{metropolis()}}, \code{\link[=particleFilter]{particleFilter()}}
}
