% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/closed-loop-cardio.R
\name{cardioRespiratory}
\alias{cardioRespiratory}
\title{Closed-loop cardiovascular-respiratory twin}
\usage{
cardioRespiratory(
  duration = 300,
  hr0 = 70,
  resp_rate = 12,
  S0 = 120,
  D0 = 80,
  R0 = 1,
  g_vagal = 0.01,
  g_symp = 0.007,
  symp_delay = 2L,
  g_resist = 0.01,
  resist_delay = 4L,
  rsa_amp = 0.035,
  resp_bp_amp = 2.5,
  starling = 0.1,
  noise = 0.02,
  respiration = TRUE,
  seed = NULL
)
}
\arguments{
\item{duration}{Recording length (s).}

\item{hr0}{Baseline heart rate (bpm).}

\item{resp_rate}{Breathing rate (breaths/min).}

\item{S0, D0}{Target systolic/diastolic pressure (mmHg).}

\item{R0}{Baseline peripheral resistance (relative).}

\item{g_vagal, g_symp}{Baroreflex heart-period gains (s/mmHg): fast vagal (current
beat) and delayed sympathetic.}

\item{symp_delay, resist_delay}{Sympathetic heart-period and resistance baroreflex
delays (beats).}

\item{g_resist}{Baroreflex resistance gain (per mmHg).}

\item{rsa_amp}{Respiratory vagal modulation of heart period (s).}

\item{resp_bp_amp}{Respiratory mechanical modulation of stroke volume (mmHg).}

\item{starling}{Frank-Starling sensitivity of stroke volume to the preceding
heart period (fractional).}

\item{noise}{SD of the beat-to-beat neural noise (fraction) that excites the loop.}

\item{respiration}{If \code{FALSE}, respiration is switched off (to show the Mayer
wave is intrinsic to the baroreflex loop, not driven by breathing).}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{cardio_respiratory} object: per-beat \code{time}, \code{rr}, \code{hr}, \code{systolic},
\code{diastolic}, \code{resistance}; the HRV and systolic-pressure LF/HF band summaries
\code{hrv} and \code{bpv} (each with \code{lf_power}/\code{hf_power}/\code{lf_peak}/\code{hf_peak}); and the
baroreflex sensitivity \code{brs} (ms/mmHg) recovered from the beat series.
}
\description{
A beat-to-beat model in which arterial pressure, heart period and peripheral
resistance evolve under a delayed, two-arm baroreflex, coupled to respiration.
A Windkessel map sets each beat's systolic and diastolic pressure; the
baroreflex adjusts the next heart period (fast vagal + delayed sympathetic) and
the peripheral resistance (delayed sympathetic) from the sensed systolic
pressure; respiration modulates stroke volume (a mechanical blood-pressure
effect) and vagal tone (respiratory sinus arrhythmia). The delayed
resistance/heart-period feedback, excited by small noise, produces an EMERGENT
~0.1 Hz Mayer wave in pressure and heart rate; respiration adds the
respiratory-frequency oscillations. None of these rhythms is imposed.
}
\examples{
cr <- cardioRespiratory(duration = 240, resp_rate = 12, seed = 1)
cr$bpv$lf_peak     # ~0.1 Hz Mayer wave in blood pressure
cr$hrv$hf_peak     # respiratory-frequency RSA in heart rate
}
\references{
de Boer RW, Karemaker JM, Strackee J (1987) Am J Physiol
253:H680-H689; Julien C (2006) Cardiovasc Res 70:12-21 (Mayer waves).
}
\seealso{
\code{\link[=windkessel]{windkessel()}}, \code{\link[=baroreflex]{baroreflex()}}, \code{\link[=rsaTachogram]{rsaTachogram()}}
}
