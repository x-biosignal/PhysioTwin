% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-filters.R
\name{ensembleKalmanFilter}
\alias{ensembleKalmanFilter}
\title{Ensemble Kalman filter (Monte-Carlo covariance state estimator)}
\usage{
ensembleKalmanFilter(f, h, y, Q, R, x0, P0, n_ensemble = 100, seed = NULL)
}
\arguments{
\item{f, h}{Process and measurement models \verb{function(x, k)}.}

\item{y}{Measurement matrix (\verb{n_steps x m}).}

\item{Q, R}{Process- and measurement-noise covariances.}

\item{x0, P0}{Initial state mean and covariance.}

\item{n_ensemble}{Ensemble size.}

\item{seed}{Optional RNG seed.}
}
\value{
an \code{enkf_result}: \code{state} (\verb{n_steps x L} ensemble means), \code{cov} (final
ensemble covariance), \code{innovation}.
}
\description{
A stochastic ensemble Kalman filter for \verb{x_k = f(x_\{k-1\}, k) + w},
\code{y_k = h(x_k, k) + v}. An ensemble of members carries the state distribution;
its sample cross- and innovation-covariances form the Kalman gain, and the
members are updated with perturbed observations. Scales to high-dimensional
states where a full covariance is impractical. Same interface as
\code{\link[=unscentedKalmanFilter]{unscentedKalmanFilter()}}.
}
\examples{
set.seed(1); y <- matrix(sin(seq(0, 6, length.out = 50)) + rnorm(50, 0, .1), 50, 1)
fit <- ensembleKalmanFilter(function(x, k) x, function(x, k) x, y,
         Q = matrix(0.05), R = matrix(0.01), x0 = 0, P0 = matrix(1), n_ensemble = 80)
fit$state[50]
}
\references{
Evensen G (1994) J Geophys Res 99:10143-10162; Burgers et al. (1998).
}
\seealso{
\code{\link[=unscentedKalmanFilter]{unscentedKalmanFilter()}}, \code{\link[=extendedKalmanFilter]{extendedKalmanFilter()}}, \code{\link[=particleFilter]{particleFilter()}}
}
