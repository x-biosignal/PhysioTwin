% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-filters.R
\name{particleFilter}
\alias{particleFilter}
\title{Bootstrap particle filter (sequential Monte-Carlo state estimator)}
\usage{
particleFilter(
  f,
  h,
  y,
  Q,
  R,
  x0,
  P0,
  n_particles = 1000,
  resample_thresh = 0.5,
  seed = NULL
)
}
\arguments{
\item{f, h}{Process and measurement models \verb{function(x, k)}.}

\item{y}{Measurement matrix (\verb{n_steps x m}).}

\item{Q, R}{Process- and measurement-noise covariances.}

\item{x0, P0}{Initial state mean and covariance.}

\item{n_particles}{Number of particles.}

\item{resample_thresh}{Resample when effective sample size \verb{< thresh * N}.}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{pf_result}: \code{state} (\verb{n_steps x L} posterior means), \code{cov} (final
weighted covariance), \code{innovation}, and \code{ess} (effective sample size per step).
}
\description{
A sequential-importance-resampling (bootstrap) particle filter for
\verb{x_k = f(x_\{k-1\}, k) + w}, \code{y_k = h(x_k, k) + v}. Particles are propagated
through \code{f} with process noise \code{Q}, weighted by the Gaussian measurement
likelihood, and resampled (systematic) when the effective sample size falls
below \code{resample_thresh} of the population. Handles strong nonlinearity and
multi-modal posteriors that a Kalman filter cannot. Same interface as
\code{\link[=unscentedKalmanFilter]{unscentedKalmanFilter()}}.
}
\examples{
set.seed(1); y <- matrix(sin(seq(0, 6, length.out = 50)) + rnorm(50, 0, .1), 50, 1)
fit <- particleFilter(function(x, k) x, function(x, k) x, y,
         Q = matrix(0.05), R = matrix(0.01), x0 = 0, P0 = matrix(1), n_particles = 500)
fit$state[50]
}
\references{
Gordon NJ, Salmond DJ, Smith AFM (1993) IEE Proc F 140:107-113.
}
\seealso{
\code{\link[=unscentedKalmanFilter]{unscentedKalmanFilter()}}, \code{\link[=extendedKalmanFilter]{extendedKalmanFilter()}}, \code{\link[=ensembleKalmanFilter]{ensembleKalmanFilter()}}
}
