% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-imu.R
\name{imuMeasure}
\alias{imuMeasure}
\title{Measure a movement twin with an IMU (forward sensor model)}
\usage{
imuMeasure(trajectory, sensor = imuSensor(), seed = NULL, noise = TRUE)
}
\arguments{
\item{trajectory}{A \code{twin_trajectory} from \code{\link[=simulateTwin]{simulateTwin()}}.}

\item{sensor}{An \code{imu_sensor} from \code{\link[=imuSensor]{imuSensor()}} (default: mild noise).}

\item{seed}{Optional RNG seed.}

\item{noise}{If \code{FALSE}, return the noise-free forward IMU (the ground truth).}
}
\value{
a data frame \verb{time, gyro, ax, ay} (angular velocity rad/s, body-frame
accelerations m/s^2).
}
\description{
Turns a simulated twin trajectory into a realistic IMU data stream by applying
the forward sensor model and the sensor's imperfections.
}
\examples{
sim <- simulateTwin(limbTwin(), duration = 2, dt = 0.01)
head(imuMeasure(sim, imuSensor(gyro_noise = 0.04), seed = 1))
}
\seealso{
\code{\link[=imuSensor]{imuSensor()}}, \code{\link[=personalizeTwin]{personalizeTwin()}}
}
