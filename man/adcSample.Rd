% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-adc.R
\name{adcSample}
\alias{adcSample}
\title{Resample a signal to a new rate with optional sampling jitter}
\usage{
adcSample(signal, fs_in, fs_out, jitter = 0)
}
\arguments{
\item{signal}{Numeric signal sampled at \code{fs_in}.}

\item{fs_in, fs_out}{Input and output sampling rates (Hz).}

\item{jitter}{Sampling-time jitter SD, in input samples.}
}
\value{
a list: \code{signal} (resampled), \code{fs} (\verb{= fs_out}), \code{time}.
}
\description{
Resamples \code{signal} from \code{fs_in} to \code{fs_out} by linear interpolation, optionally
perturbing each output sample time by Gaussian timing \code{jitter} -- the aperture
jitter of a real converter's clock.
}
\examples{
down <- adcSample(sin(seq(0, 12, length.out = 2000)), fs_in = 1000, fs_out = 250)
down$fs
}
\seealso{
\code{\link[=adcQuantize]{adcQuantize()}}
}
