% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator-network.R
\name{kuramoto}
\alias{kuramoto}
\title{Kuramoto coupled-oscillator network}
\usage{
kuramoto(n = 20, K = 1, omega = NULL, duration = 20, dt = 0.01, seed = NULL)
}
\arguments{
\item{n}{Number of oscillators.}

\item{K}{Coupling strength.}

\item{omega}{Natural frequencies (length \code{n}); default \code{rnorm(n)}.}

\item{duration, dt}{Simulation length and step (s).}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{kuramoto} object: \code{time}, \code{phase} (\verb{nt x n}), \code{order} (order
parameter per step), \code{signals} (\code{cos(phase)}), \code{mean_order}.
}
\description{
Simulates \code{n} phase oscillators with natural frequencies \code{omega} and all-to-all
coupling \code{K} (\verb{theta_i' = omega_i + K/n sum_j sin(theta_j - theta_i)}). The
order parameter measures collective synchronisation: near 0 (incoherent) below
the critical coupling, near 1 (synchronised) above it.
}
\examples{
kuramoto(n = 15, K = 3, duration = 20, seed = 1)$mean_order   # synchronised
kuramoto(n = 15, K = 0, duration = 20, seed = 1)$mean_order   # incoherent
}
\references{
Kuramoto Y (1975); Strogatz SH (2000) Physica D 143:1-20.
}
\seealso{
\code{\link[=cpgMatsuoka]{cpgMatsuoka()}}, \code{\link[=ecgsyn]{ecgsyn()}}, \code{\link[=jansenRit]{jansenRit()}}
}
