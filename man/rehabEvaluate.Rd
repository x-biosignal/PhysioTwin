% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rehab-bridge.R
\name{rehabEvaluate}
\alias{rehabEvaluate}
\title{Evaluate a twin's intervention prediction against a real single-case series}
\usage{
rehabEvaluate(
  baseline,
  intervention,
  predicted_delta = NULL,
  measure = "gait_speed",
  mcid = 0.16,
  direction = "increase",
  condition = "in-silico twin",
  patient_id = "twin",
  start_date = "2026-01-01"
)
}
\arguments{
\item{baseline}{Numeric outcome measurements in the baseline phase.}

\item{intervention}{Numeric outcome measurements in the intervention phase.}

\item{predicted_delta}{Optional: the twin's predicted change in the outcome
(e.g. from \code{\link[=scoreInterventions]{scoreInterventions()}} / \code{\link[=evaluateIntervention]{evaluateIntervention()}}), for reconciliation.}

\item{measure}{Outcome name (a PhysioRehab measure, e.g. \code{"gait_speed"},
\code{"cadence"}; drives the ICF mapping).}

\item{mcid}{Minimal clinically important difference (threshold, outcome units).}

\item{direction}{\code{"increase"} or \code{"decrease"} -- the improving direction.}

\item{condition, patient_id}{Episode metadata.}

\item{start_date}{First session date (\code{Date} or a string coercible by \code{\link[=as.Date]{as.Date()}}).}
}
\value{
a \code{rehab_evaluation}: the built \code{episode}, the PhysioRehab \code{sced} result,
the \code{icf} code(s), and -- when \code{predicted_delta} is given -- a \code{reconciliation}
(\code{predicted}, \code{observed}, \code{sign_agree}, \code{mcid_agree}, \code{error}).
}
\description{
Connects the digital-twin decision loop to the PhysioRehab clinical-reasoning
framework. A real single-case series -- outcome measurements in a \code{baseline}
phase then an \code{intervention} phase -- is assembled into a \code{rehab_episode} and
analysed with PhysioRehab's single-case experimental design engine: the NAP and
Tau effect sizes, whether the change exceeds the \code{mcid}, and the ICF category of
the \code{measure}. If the twin's \code{predicted_delta} is supplied, it is reconciled
with the clinically-observed change (do they agree in sign, and on the MCID
verdict?), closing the predict -> deliver -> evaluate loop against real evidence.
}
\examples{
if (requireNamespace("PhysioRehab", quietly = TRUE)) {
  set.seed(1)
  base <- rnorm(5, 0.80, 0.03); post <- rnorm(6, 1.00, 0.03)
  rehabEvaluate(base, post, predicted_delta = 0.18, measure = "gait_speed", mcid = 0.16)
}
}
\seealso{
\code{\link[=evaluateIntervention]{evaluateIntervention()}}, \code{\link[=clinicalDecision]{clinicalDecision()}}
}
