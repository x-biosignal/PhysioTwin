% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cardio-statespace.R
\name{personalizeCardioWaveform}
\alias{personalizeCardioWaveform}
\title{Personalise the closed-loop cardiovascular twin from the whole RR record}
\usage{
personalizeCardioWaveform(
  rr,
  resp_freq = 0.25,
  estimate = NULL,
  lf_model = c("oscillator", "ar1"),
  fixed = list(omega_m = 0.53, rr0 = NULL, q_mayer = 1, r_obs = 9),
  n_iter = 4000,
  seed = NULL
)
}
\arguments{
\item{rr}{RR-interval series (ms).}

\item{resp_freq}{Respiratory frequency (Hz) for the RSA regressor.}

\item{estimate}{Parameters to fit (any of \code{"g"}, \code{"rho"}, \code{"A_rsa"}, \code{"phi"});
\code{NULL} picks the default for the \code{lf_model} (\code{g}/\code{rho}/\code{A_rsa} for the
oscillator, \code{g}/\code{phi}/\code{A_rsa} for \code{"ar1"}).}

\item{lf_model}{Low-frequency model (see \code{\link[=cardioStateSpace]{cardioStateSpace()}}): \code{"oscillator"}
(a resonant Mayer peak) or \code{"ar1"} (broadband low-frequency power, for records
without a distinct Mayer rhythm, e.g. a regular healthy sinus rhythm).}

\item{fixed}{Named list of held-fixed \code{\link[=cardioStateSpace]{cardioStateSpace()}} parameters
(\code{omega_m}, \code{rr0}, \code{q_mayer}, \code{r_obs}, and any low-frequency parameter not fitted).}

\item{n_iter}{Metropolis iterations.}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{cardio_waveform_fit}: the \code{posterior} (an \code{mcmc_result}), the fitted
\code{estimates}, and the \code{estimate}d names.
}
\description{
Fits the \code{\link[=cardioStateSpace]{cardioStateSpace()}} parameters jointly to a whole RR-interval record.
Because the closed-loop model is linear-Gaussian its likelihood is exact (a
Kalman filter), so the posterior is sampled by \code{\link[=metropolis]{metropolis()}} -- the
whole-waveform counterpart to the summary-based \code{\link[=personalizeCardio]{personalizeCardio()}}. The Mayer
gain/damping and the respiratory amplitude are identifiable here because the
record separates the low- and high-frequency rhythms, resolving the ridge that
confounds the summary fit. (For an intractable, nonlinear model use
\code{\link[=particleMCMC]{particleMCMC()}} / \code{\link[=particleGibbs]{particleGibbs()}} with \code{\link[=cardioStateSpace]{cardioStateSpace()}}'s \code{f}/\code{h}.)
}
\examples{
\donttest{
truth <- cardioStateSpace(g = 8, rho = 0.9, A_rsa = 20,
                          resp = sin(2 * pi * 0.25 * cumsum(rep(0.85, 250))))
set.seed(1); x <- c(0, 0); rr <- numeric(250)
for (k in seq_len(250)) {
  x <- truth$f(x, k) + c(rnorm(1), 0); rr[k] <- truth$h(x, k) + rnorm(1, 0, 3)
}
fit <- personalizeCardioWaveform(rr, resp_freq = 0.25, estimate = c("g", "A_rsa"),
                                 n_iter = 3000, seed = 1)
fit$estimates
}
}
\seealso{
\code{\link[=cardioStateSpace]{cardioStateSpace()}}, \code{\link[=metropolis]{metropolis()}}, \code{\link[=particleMCMC]{particleMCMC()}}, \code{\link[=personalizeCardio]{personalizeCardio()}}
}
