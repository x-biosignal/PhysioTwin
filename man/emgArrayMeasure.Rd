% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-crosstalk.R
\name{emgArrayMeasure}
\alias{emgArrayMeasure}
\title{Measure per-muscle activations with a surface-EMG array (forward model)}
\usage{
emgArrayMeasure(activations, sensor = emgArray(), seed = NULL)
}
\arguments{
\item{activations}{An \verb{n_time x channels} matrix of non-negative activation
envelopes (one column per muscle/channel).}

\item{sensor}{An \code{emg_array} from \code{\link[=emgArray]{emgArray()}}.}

\item{seed}{Optional RNG seed.}
}
\value{
an \code{emg_array_measurement}: \code{raw} (\verb{n_time x channels}), \code{clean}
(uncontaminated, un-mixed, amplified), \code{time}, \code{fs}, the \code{crosstalk} matrix, sensor.
}
\description{
Turns a matrix of per-muscle activation envelopes into the multi-channel raw
sEMG an electrode array records: each channel's activation-modulated Gaussian
sEMG is band-limited, the channels are MIXED through the crosstalk matrix, and
the amplifier adds CMRR-rejected mains interference, a DC-blocking high-pass,
thermal noise and gain.
}
\examples{
act <- matrix(pmax(0, sin(outer(seq(0, 6, len = 1200), 1:3))), ncol = 3)
m <- emgArrayMeasure(act, emgArray(channels = 3, crosstalk = 0.2), seed = 1)
dim(m$raw)
}
\seealso{
\code{\link[=emgArray]{emgArray()}}, \code{\link[=crosstalkMatrix]{crosstalkMatrix()}}
}
