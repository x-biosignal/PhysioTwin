% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cardio-statespace.R
\name{cardioStateSpace}
\alias{cardioStateSpace}
\title{Closed-loop cardiovascular state-space model (for whole-record fitting)}
\usage{
cardioStateSpace(
  g = 8,
  rho = 0.9,
  omega_m = 0.53,
  A_rsa = 20,
  resp,
  rr0 = 850,
  q_mayer = 1,
  r_obs = 9,
  lf_model = c("oscillator", "ar1"),
  phi = 0.9
)
}
\arguments{
\item{g}{Low-frequency gain onto the RR interval (ms per unit state).}

\item{rho}{Mayer-oscillator damping (0..1; nearer 1 = sharper low-frequency peak;
used when \code{lf_model = "oscillator"}).}

\item{omega_m}{Mayer angular frequency (radians per beat; \code{"oscillator"} only).}

\item{A_rsa}{Respiratory (RSA) amplitude on the RR interval (ms).}

\item{resp}{Respiratory regressor sampled at the beats (length = number of beats).}

\item{rr0}{Mean RR interval (ms).}

\item{q_mayer, r_obs}{Low-frequency process-noise and RR observation-noise variances.}

\item{lf_model}{Low-frequency model: \code{"oscillator"} (a resonant, second-order
damped Mayer oscillator, for records with a distinct Mayer peak) or \code{"ar1"} (a
first-order autoregressive process giving BROADBAND low-frequency power without a
peak, for records whose low-frequency variability is not a sharp rhythm).}

\item{phi}{First-order autoregressive coefficient (0..1; used when
\code{lf_model = "ar1"}); nearer 1 = more low-frequency power.}
}
\value{
a state-space model list \code{f}, \code{h} (\verb{function(x, k)}), \code{Q}, \code{R}, \code{x0}, \code{P0}.
}
\description{
A linear-Gaussian state-space representation of the RR-interval series: a
damped second-order Mayer oscillator (the delayed-baroreflex resonance) drives
the heart period through a gain \code{g}, and respiration adds a high-frequency
(respiratory sinus arrhythmia) term of amplitude \code{A_rsa}. Suitable as the
\code{build_model} argument of \code{\link[=particleMCMC]{particleMCMC()}} / \code{\link[=particleGibbs]{particleGibbs()}}.
}
\examples{
resp <- sin(2 * pi * 0.25 * cumsum(rep(0.85, 200)))
m <- cardioStateSpace(g = 8, rho = 0.9, omega_m = 0.53, A_rsa = 20, resp = resp)
m$h(c(1, 0), 1)
}
\seealso{
\code{\link[=personalizeCardioWaveform]{personalizeCardioWaveform()}}, \code{\link[=particleMCMC]{particleMCMC()}}, \code{\link[=cardioRespiratory]{cardioRespiratory()}}
}
