% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surrogate-gp.R
\name{gpEmulator}
\alias{gpEmulator}
\title{Gaussian-process emulator (surrogate model)}
\usage{
gpEmulator(
  X,
  y,
  optimize = TRUE,
  lengthscale = NULL,
  sigma_f = NULL,
  sigma_n = NULL
)
}
\arguments{
\item{X}{Training inputs: a matrix (\verb{n x d}) or length-\code{n} vector.}

\item{y}{Training outputs (length \code{n}).}

\item{optimize}{If \code{TRUE}, fit hyperparameters by marginal likelihood; otherwise
use the initial values.}

\item{lengthscale, sigma_f, sigma_n}{Initial (or fixed) kernel length-scale,
signal SD and noise SD; \code{NULL} picks data-driven defaults (median pairwise
distance, \code{sd(y)}, a small nugget).}
}
\value{
a \code{gpEmulator} object (with a \code{\link[=predict.gpEmulator]{predict.gpEmulator()}} method): the fitted
\code{lengthscale}, \code{sigma_f}, \code{sigma_n} and the training data.
}
\description{
Fits a Gaussian-process regression surrogate with a squared-exponential kernel
to training inputs \code{X} and outputs \code{y} -- typically a design of simulator runs,
so the emulator stands in for an expensive twin. Hyperparameters (length-scale,
signal and noise SD) are fitted by maximising the marginal likelihood.
\code{\link[=predict.gpEmulator]{predict.gpEmulator()}} returns the posterior mean and standard deviation at new
inputs; the SD is small near training points and grows away from them.
}
\examples{
set.seed(1); x <- runif(20, 0, 6); y <- sin(x) + rnorm(20, 0, 0.05)
gp <- gpEmulator(x, y)
p <- predict(gp, seq(0, 6, length.out = 5)); round(p$mean, 2)
}
\references{
Rasmussen CE, Williams CKI (2006) Gaussian Processes for Machine Learning.
}
\seealso{
\code{\link[=predict.gpEmulator]{predict.gpEmulator()}}, \code{\link[=sobolIndices]{sobolIndices()}}, \code{\link[=abcCalibration]{abcCalibration()}}
}
