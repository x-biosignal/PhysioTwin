% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator-cardiovascular.R
\name{baroreflex}
\alias{baroreflex}
\title{Baroreflex closed-loop blood-pressure regulation}
\usage{
baroreflex(
  duration = 60,
  setpoint = 92,
  gain = 0.02,
  hr0 = 70,
  stroke_volume = 80,
  R = 1,
  C = 1.9,
  Zc = 0.05,
  systolic_frac = 0.3,
  perturb_time = 30,
  perturb_R = 0.7,
  reflex = TRUE,
  sfp = 250
)
}
\arguments{
\item{duration}{Recording length (s).}

\item{setpoint}{Regulated mean-arterial-pressure set-point (mmHg).}

\item{gain}{Baroreflex sensitivity, RR change per unit pressure error (s/mmHg).}

\item{hr0}{Baseline heart rate (bpm).}

\item{stroke_volume, R, C, Zc, systolic_frac}{Windkessel parameters (see \code{\link[=windkessel]{windkessel()}}).}

\item{perturb_time}{Time of the resistance step (s); \code{Inf} for no perturbation.}

\item{perturb_R}{Multiplier applied to \code{R} from \code{perturb_time} on (e.g. 0.7 =
30\% vasodilation).}

\item{reflex}{If \code{FALSE}, the RR interval is held at baseline (open loop) -- the
control for demonstrating regulation.}

\item{sfp}{Output sampling rate (Hz).}
}
\value{
a \code{baroreflex} object: continuous \code{time}/\code{pressure}; per-beat
\code{beat_time}, \code{rr}, \code{hr}, \code{beat_map}; the \code{setpoint}; and \code{regulated} (the
post-perturbation steady-state absolute MAP error).
}
\description{
Closes the short-term baroreflex around the \code{\link[=windkessel]{windkessel()}} arterial model:
each beat the mean arterial pressure is compared to a set-point and the next
heart period (RR interval) is set to \code{RR0 + gain * (MAP - setpoint)} -- a
higher pressure lengthens RR (slows the heart), lowering cardiac output and
buffering the pressure (negative feedback; \code{gain} is the baroreflex
sensitivity, s/mmHg). A step change in peripheral resistance at \code{perturb_time}
(e.g. vasodilation) challenges the loop; with the reflex engaged the mean
pressure is actively regulated back toward the set-point.
}
\examples{
br <- baroreflex(duration = 60, perturb_time = 30, perturb_R = 0.7)
br$regulated       # small residual MAP error -- pressure is buffered
}
\references{
Ursino M (1998) Am J Physiol 275:H1733-H1747. deBoer RW et al.
(1987) Am J Physiol 253:H680-H689.
}
\seealso{
\code{\link[=windkessel]{windkessel()}}, \code{\link[=rsaTachogram]{rsaTachogram()}}
}
