% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical-decision.R
\name{twinReadiness}
\alias{twinReadiness}
\title{Certify a personalised twin before it is allowed to advise (validation gate)}
\usage{
twinReadiness(
  fit,
  imu,
  dt = 0.005,
  gyro_var = 0.03^2,
  accel_var = 0.15^2,
  cv_warn = 0.25,
  chi2_ready = 3,
  chi2_bad = 10
)
}
\arguments{
\item{fit}{A \code{personalized_twin} from \code{\link[=personalizeTwin]{personalizeTwin()}}.}

\item{imu}{The IMU data the twin was fitted to.}

\item{dt}{Sample period (s).}

\item{gyro_var, accel_var}{Measurement-noise variances (match the fit).}

\item{cv_warn}{Log-space parameter SD above which a parameter is deemed poorly
identified.}

\item{chi2_ready, chi2_bad}{Reduced-chi-square thresholds: below \code{chi2_ready} the
fit is good (a candidate for \code{ready}); above \code{chi2_bad} it is a misfit
(\code{not_ready}).}
}
\value{
a \code{twin_readiness}: \code{verdict}, \code{reduced_chi2} (goodness of fit across
ALL channels), \code{gyro_nrmse}, the (misfit-inflated) parameter \code{cov}, per-
parameter \code{cv}, \code{confidence}, and the \code{estimates}/\code{theta0}/\code{gyro_bias}.
}
\description{
The trust gate of the clinical loop. A personalised twin may only inform
decisions once it is shown to (1) FIT the patient's data and (2) have
IDENTIFIABLE parameters. This recomputes the personalisation residual (fit
quality) and a Fisher-information parameter covariance (identifiability), and
returns a verdict (\code{ready} / \code{provisional} / \code{not_ready}) with a \verb{[0, 1]}
confidence that downstream scoring uses to scale and flag its recommendations.
}
\examples{
\donttest{
truth <- limbTwin(strength = 7, damping = 0.5)
imu <- imuMeasure(simulateTwin(truth, 6, 0.01), imuSensor(gyro_noise = 0.03), seed = 1)
fit <- personalizeTwin(imu, limbTwin(strength = 4), estimate = "strength", dt = 0.01)
twinReadiness(fit, imu, dt = 0.01)$verdict
}
}
\references{
Sargent RG (2013) J Simul 7:12-24 (model validation); Bevington &
Robinson (reduced chi-square goodness of fit); Cramer-Rao identifiability.
}
\seealso{
\code{\link[=personalizeTwin]{personalizeTwin()}}, \code{\link[=scoreInterventions]{scoreInterventions()}}, \code{\link[=validateTwin]{validateTwin()}}
}
