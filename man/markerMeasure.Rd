% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-marker.R
\name{markerMeasure}
\alias{markerMeasure}
\title{Measure a marker trajectory with an optical mocap system}
\usage{
markerMeasure(position, sensor = markerSensor(), seed = NULL)
}
\arguments{
\item{position}{Clean marker trajectory: an \verb{n x d} matrix (or length-\code{n}
vector) of marker positions over time.}

\item{sensor}{A \code{marker_sensor} from \code{\link[=markerSensor]{markerSensor()}}.}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{marker_measurement}: \code{position} (measured, \code{NA} rows where occluded),
\code{visible} (logical per frame), \code{gap_fraction}, and the \code{sensor}.
}
\description{
Applies the forward marker sensor model to a clean trajectory: adds Gaussian
noise, carves contiguous occlusion gaps (visible/occluded Markov process),
optionally quantises and injects ghost samples. Occluded frames are returned as
\code{NA} rows.
}
\examples{
pos <- cbind(sin(seq(0, 6, length.out = 300)), cos(seq(0, 6, length.out = 300)))
m <- markerMeasure(pos, markerSensor(dropout_prob = 0.02, dropout_len = 12), seed = 1)
m$gap_fraction
}
\seealso{
\code{\link[=markerSensor]{markerSensor()}}, \code{\link[=fillMarkerGaps]{fillMarkerGaps()}}
}
