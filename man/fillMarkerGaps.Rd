% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-marker.R
\name{fillMarkerGaps}
\alias{fillMarkerGaps}
\title{Fill occlusion gaps in a measured marker trajectory}
\usage{
fillMarkerGaps(m)
}
\arguments{
\item{m}{a \code{marker_measurement} from \code{\link[=markerMeasure]{markerMeasure()}}, or a numeric matrix
with \code{NA} rows.}
}
\value{
the trajectory matrix with gaps interpolated.
}
\description{
Linear-interpolates the \code{NA} runs (occlusions) of a \code{\link[=markerMeasure]{markerMeasure()}} result,
column by column, holding the endpoints flat where a gap reaches the boundary.
}
\examples{
pos <- matrix(cumsum(rnorm(200)), 200, 1)
filled <- fillMarkerGaps(markerMeasure(pos, markerSensor(dropout_prob = 0.05), seed = 1))
}
\seealso{
\code{\link[=markerMeasure]{markerMeasure()}}
}
