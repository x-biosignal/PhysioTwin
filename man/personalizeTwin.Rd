% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-ukf.R
\name{personalizeTwin}
\alias{personalizeTwin}
\title{Personalise a movement twin to recorded IMU data}
\usage{
personalizeTwin(
  imu,
  prior,
  estimate = "strength",
  method = c("optim", "ukf"),
  dt = 0.005,
  param_rw = 0.02,
  gyro_var = 0.03^2,
  accel_var = 0.15^2,
  burn_in = 0.5,
  n_starts = 25L
)
}
\arguments{
\item{imu}{A data frame with \verb{gyro, ax, ay} (as from \code{\link[=imuMeasure]{imuMeasure()}}), sampled
at \code{dt}.}

\item{prior}{A \code{movement_twin} giving the fixed parameters and the initial
guess for the estimated ones.}

\item{estimate}{Character vector of parameters to personalise (any of
\code{"strength"}, \code{"damping"}, \code{"stiffness"}, \code{"inertia"}).}

\item{method}{\code{"optim"} (default) fits the whole record by minimising the
simulated-vs-observed IMU error (a batch/variational fit, robust to strong
non-linearity); \code{"ukf"} runs the recursive unscented Kalman filter (online,
best in the mildly non-linear regime).}

\item{dt}{Sample period (s).}

\item{param_rw}{Random-walk SD (log-space) for the estimated parameters
(their assumed drift; default 0.02).}

\item{gyro_var, accel_var}{Measurement-noise variances for the gyro and accel
channels.}

\item{burn_in}{Fraction of the record discarded before averaging the converged
estimate (default 0.5).}

\item{n_starts}{Number of coarse-grid starting points scanned before the
\code{"optim"} fit (multistart). The objective is evaluated on a multiplicative
grid around the prior (one cheap simulation each) and the fit is started from
the best point, making multi-parameter fits robust to spurious co-estimation
minima at negligible cost; \code{1} reproduces a single Nelder-Mead start from the
prior.}
}
\value{
a \code{personalized_twin}: \code{twin} (the fitted twin), \code{estimates} (named
fitted parameters), the co-estimated \code{gyro_bias}, and -- for \code{method = "ukf"} -- the per-step \code{trajectory} and the \code{ukf} result, or -- for \code{method = "optim"} -- the fitted \code{theta0} and final \code{objective}.
}
\description{
Estimates a subject's twin parameters by assimilating their IMU stream with an
unscented Kalman filter over an augmented state \verb{[theta, omega, log(params)]}.
Parameters are estimated in log-space (keeping them positive) and the fitted
values are read from the converged filter.
}
\examples{
\donttest{
truth <- limbTwin(strength = 7, damping = 0.5)
sim <- simulateTwin(truth, duration = 6, dt = 0.01)
imu <- imuMeasure(sim, imuSensor(gyro_noise = 0.03, accel_noise = 0.1), seed = 1)
fit <- personalizeTwin(imu, limbTwin(strength = 4, damping = 0.2),
                       estimate = c("strength", "damping"), dt = 0.01)
fit$estimates
}
}
\seealso{
\code{\link[=unscentedKalmanFilter]{unscentedKalmanFilter()}}, \code{\link[=extendedKalmanFilter]{extendedKalmanFilter()}}, \code{\link[=particleFilter]{particleFilter()}},
\code{\link[=abcCalibration]{abcCalibration()}}, \code{\link[=insilicoIntervention]{insilicoIntervention()}}
}
