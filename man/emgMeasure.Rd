% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-emg.R
\name{emgMeasure}
\alias{emgMeasure}
\title{Measure muscle activation with a surface-EMG electrode (forward model)}
\usage{
emgMeasure(activation, sensor = emgElectrode(), seed = NULL)
}
\arguments{
\item{activation}{Non-negative activation envelope (0..1), length \code{n}.}

\item{sensor}{An \code{emg_electrode} from \code{\link[=emgElectrode]{emgElectrode()}}.}

\item{seed}{Optional RNG seed.}
}
\value{
an \code{emg_measurement}: \code{raw} (contaminated, amplified sEMG), \code{clean}
(uncontaminated band-limited EMG, amplified), \code{time}, \code{activation}, \code{fs}, \code{sensor}.
}
\description{
Turns an activation envelope into a realistic raw sEMG: an activation-modulated
Gaussian process band-passed to the electrode's passband (the clean EMG), then
contaminated with powerline interference, baseline/motion wander and thermal
noise and amplified.
}
\examples{
act <- pmax(0, sin(seq(0, 4 * pi, length.out = 2000)))
emg <- emgMeasure(act, emgElectrode(fs = 1000), seed = 1)
cor(emgEnvelope(emg$clean, 1000), act)
}
\seealso{
\code{\link[=emgElectrode]{emgElectrode()}}, \code{\link[=emgEnvelope]{emgEnvelope()}}
}
