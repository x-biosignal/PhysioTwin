% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/personalize-cardio.R
\name{personalizeCardio}
\alias{personalizeCardio}
\title{Personalise the closed-loop CV-respiratory twin to real HRV/BPV summaries}
\usage{
personalizeCardio(
  target,
  ranges = list(g_vagal = c(0.004, 0.02), g_resist = c(0.003, 0.02), rsa_amp = c(0.01,
    0.06)),
  fixed = list(),
  duration = 300,
  n_init = 12,
  n_iter = 25,
  n_rep = 3,
  seed = 1
)
}
\arguments{
\item{target}{Named numeric vector of the observed summaries to match; any of
\code{brs}, \code{bp_lf}, \code{hr_hf} (see \code{\link[=cardioSummary]{cardioSummary()}}).}

\item{ranges}{Named list of \code{c(lower, upper)} bounds for the parameters to fit
(default \code{g_vagal}, \code{g_resist}, \code{rsa_amp}).}

\item{fixed}{Named list of other \code{\link[=cardioRespiratory]{cardioRespiratory()}} parameters held fixed.}

\item{duration}{Simulation length per evaluation (s).}

\item{n_init, n_iter}{Bayesian-optimisation initial design and iterations.}

\item{n_rep}{Simulations averaged per evaluation to stabilise the (stochastic)
variability summaries -- the blood-pressure Mayer power is noisy, so averaging
makes the inverse problem well-posed.}

\item{seed}{RNG seed (fixes the stochastic loss so the calibration is deterministic).}
}
\value{
a \code{cardio_personalization}: the fitted \code{estimates}, the personalised
\code{twin}, the \code{target} and \code{achieved} summaries, the relative error \code{rel_error},
and the \code{calibration} object.
}
\description{
Fits the closed-loop \code{\link[=cardioRespiratory]{cardioRespiratory()}} model's baroreflex gains and
respiratory modulation to a subject's measured variability summaries (baroreflex
sensitivity, blood-pressure Mayer power, heart-rate respiratory power) by
GP-accelerated calibration (\code{\link[=gpCalibrate]{gpCalibrate()}}). Because the summaries have very
different scales, the discrepancy is relative. The returned twin, driven with
the fitted parameters, reproduces the subject's HRV/BPV.
}
\examples{
\donttest{
subject <- cardioSummary(cardioRespiratory(duration = 240, g_vagal = 0.013,
                         g_resist = 0.008, rsa_amp = 0.045, seed = 3))
fit <- personalizeCardio(subject, n_init = 8, n_iter = 15, seed = 1)
fit$achieved            # reproduces the subject's brs / bp_lf / hr_hf
}
}
\seealso{
\code{\link[=cardioRespiratory]{cardioRespiratory()}}, \code{\link[=cardioSummary]{cardioSummary()}}, \code{\link[=gpCalibrate]{gpCalibrate()}}, \code{\link[=personalizeTwin]{personalizeTwin()}}
}
