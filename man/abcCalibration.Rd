% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-bayes.R
\name{abcCalibration}
\alias{abcCalibration}
\title{Approximate Bayesian Computation (likelihood-free parameter calibration)}
\usage{
abcCalibration(
  simulate,
  observed,
  prior,
  n = 5000,
  accept = 0.02,
  regression = TRUE,
  seed = NULL
)
}
\arguments{
\item{simulate}{\verb{function(theta)} returning a numeric vector of summary
statistics for a parameter vector \code{theta}.}

\item{observed}{Observed summary statistics (same length as \code{simulate}'s output).}

\item{prior}{\verb{function()} returning one prior draw (a named numeric vector).}

\item{n}{Number of prior simulations.}

\item{accept}{Fraction of draws to retain (the ABC tolerance as a quantile).}

\item{regression}{If \code{TRUE}, apply the Beaumont local-linear regression adjustment.}

\item{seed}{Optional RNG seed.}
}
\value{
an \code{abc_posterior}: \code{samples} (accepted, adjusted parameter draws),
\code{raw} (accepted draws before adjustment), \code{mean}, \code{sd}, \code{quantiles} (2.5/50/
97.5\%), \code{threshold} (accepted distance) and \code{accept_rate}.
}
\description{
Calibrates a simulator's parameters to observed data without a likelihood:
parameters are drawn from \code{prior}, passed through \code{simulate} to produce summary
statistics, and the draws whose summaries fall closest to \code{observed} (the
smallest-distance \code{accept} fraction) are retained as posterior samples.
Distances are Euclidean on summaries scaled by their simulated SD, so
statistics on different scales contribute comparably. With \code{regression = TRUE}
the accepted draws are corrected by a distance-weighted local-linear regression
onto the observed summaries.
}
\examples{
# recover the mean/SD of a Gaussian from its sample mean and SD
set.seed(1); obs <- c(mean = 2, sd = 1.5)
sim <- function(th) { x <- rnorm(200, th[1], th[2]); c(mean(x), sd(x)) }
pri <- function() c(m = runif(1, -3, 7), s = runif(1, 0.2, 4))
post <- abcCalibration(sim, obs, pri, n = 2000, accept = 0.02, seed = 1)
post$mean
}
\references{
Beaumont MA, Zhang W, Balding DJ (2002) Genetics 162:2025-2035;
Pritchard JK et al. (1999).
}
\seealso{
\code{\link[=personalizeTwin]{personalizeTwin()}}, \code{\link[=profileLikelihood]{profileLikelihood()}}, \code{\link[=gpEmulator]{gpEmulator()}}
}
