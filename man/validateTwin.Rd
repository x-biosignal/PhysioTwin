% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validateTwin}
\alias{validateTwin}
\title{Validity assessment of a movement twin (verification & validation)}
\usage{
validateTwin(
  twin,
  estimate = c("strength", "damping"),
  sensor = imuSensor(gyro_noise = 0.03, accel_noise = 0.12),
  duration = 6,
  dt = 0.02,
  theta0 = 0.2,
  n_mc = 15L,
  n_traj = 8L,
  method = "optim",
  checks = c("verification", "identifiability", "recovery", "sensitivity", "prediction",
    "domain")
)
}
\arguments{
\item{twin}{A \code{movement_twin} (the ground-truth model to assess).}

\item{estimate}{Parameters whose identifiability/recovery are assessed.}

\item{sensor}{The \code{imu_sensor} used for the recovery / prediction checks.}

\item{duration, dt, theta0}{Simulation settings for the checks.}

\item{n_mc, n_traj}{Monte-Carlo recovery repetitions and Morris trajectories.}

\item{method}{Assimilation method for the recovery check (\code{"optim"}/\code{"ukf"}).}

\item{checks}{Which layers to run (subset of \code{c("verification", "identifiability", "recovery", "sensitivity", "prediction", "domain")}).}
}
\value{
a \code{twin_validity} report.
}
\description{
Runs the applicable simulation-validity checks and returns a structured
report: verification (energy conservation, integrator order), identifiability
(can parameters be recovered in principle), Monte-Carlo recovery (bias / SD /
coverage), sensitivity screening (which parameters matter), cross-condition
prediction + face validity, and the domain of applicability. This is
verification and internal validation on the model itself; validation against a
subject's REAL data is done separately by personalising and predicting held-out
recordings.
}
\examples{
\donttest{
rep <- validateTwin(limbTwin(strength = 6, damping = 0.5, stiffness = 6),
                    estimate = c("strength", "damping"), n_mc = 8, n_traj = 5)
rep$verification
}
}
\references{
Sargent RG (2013) J Simulation 7:12-24; ASME V&V 10/40;
Oberkampf & Roy (2010) Verification and Validation in Scientific Computing.
}
\seealso{
\code{\link[=personalizeTwin]{personalizeTwin()}}, \code{\link[=insilicoIntervention]{insilicoIntervention()}}
}
