% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervention-population.R
\name{populationIntervention}
\alias{populationIntervention}
\title{In-silico intervention trial over a population of twins}
\usage{
populationIntervention(
  n = 200,
  params = list(strength = c(5, 1), damping = c(0.7, 0.2), stiffness = c(2.5, 0.6)),
  intervention = list(scale = list(strength = 1.4)),
  outcome = "rom",
  mcid = NULL,
  duration = 6,
  dt = 0.005,
  theta0 = 0.1,
  omega0 = 0,
  seed = NULL
)
}
\arguments{
\item{n}{Number of subjects (twins) in the cohort.}

\item{params}{Named list of \code{c(mean, sd)} population distributions for
\code{\link[=limbTwin]{limbTwin()}} parameters (sampled Gaussian, truncated positive).}

\item{intervention}{List with \code{scale} and/or \code{set} (as \code{\link[=insilicoIntervention]{insilicoIntervention()}}).}

\item{outcome}{Outcome to track: one of \code{"rom"}, \code{"peak_velocity"},
\code{"mean_speed"}, \code{"peak_accel"}, or a \verb{function(trajectory)} returning a scalar.}

\item{mcid}{Minimal clinically important difference (responder threshold);
defaults to 0.1 x the baseline outcome SD.}

\item{duration, dt, theta0, omega0}{Simulation settings (as \code{\link[=simulateTwin]{simulateTwin()}}).}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{population_intervention}: \code{subjects} (per-subject params, \code{pre},
\code{post}, \code{delta}), \code{effect} (mean delta, SD, Cohen's \code{d_z}), \code{responder_rate},
\code{mcid}, \code{power} and \code{n_for_80}.
}
\description{
Samples a cohort of movement twins from a parameter distribution, applies the
same intervention to each (via \code{\link[=insilicoIntervention]{insilicoIntervention()}}), and summarises the
response as a paired in-silico trial: the mean effect and its paired effect size
(Cohen's \code{d_z}), the responder rate against a minimal clinically important
difference, the statistical power at this sample size and the sample size needed
for 80\% power.
}
\examples{
\donttest{
pop <- populationIntervention(n = 100, intervention = list(scale = list(strength = 1.4)),
                              seed = 1)
pop$effect
}
}
\seealso{
\code{\link[=insilicoIntervention]{insilicoIntervention()}}, \code{\link[=multimodalTwin]{multimodalTwin()}}, \code{\link[=generateTrainingData]{generateTrainingData()}}
}
