% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate-gp.R
\name{gpCalibrate}
\alias{gpCalibrate}
\title{GP-accelerated calibration (Bayesian optimisation of an expensive loss)}
\usage{
gpCalibrate(
  loss,
  ranges,
  n_init = 12,
  n_iter = 20,
  xi = 0.01,
  n_cand = 500,
  seed = NULL
)
}
\arguments{
\item{loss}{\verb{function(theta)} returning the scalar loss to MINIMISE (e.g. the
distance between simulated and observed summary statistics).}

\item{ranges}{Named list of \code{c(lower, upper)} bounds, one per parameter.}

\item{n_init}{Number of initial random design points.}

\item{n_iter}{Number of Bayesian-optimisation iterations (loss evaluations after
the initial design).}

\item{xi}{Expected-improvement exploration margin.}

\item{n_cand}{Candidate points scored by expected improvement each iteration.}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{gp_calibration}: the best \code{par} and \code{value}, the full design
\code{X}/\code{y}, the best-so-far \code{trace}, and the final \code{gp} surrogate.
}
\description{
Minimises an expensive loss over a bounded parameter box by Bayesian
optimisation: it fits a \code{\link[=gpEmulator]{gpEmulator()}} surrogate to the evaluated
(parameters, loss) points and repeatedly evaluates the loss at the point of
maximum expected improvement. Far fewer loss evaluations than grid search or a
local optimiser -- the tool for calibrating a slow simulator to data.
}
\examples{
# recover the minimum of a 2-D loss in a few evaluations
loss <- function(p) (p[1] - 0.3)^2 + (p[2] - 0.7)^2
cal <- gpCalibrate(loss, list(x = c(0, 1), y = c(0, 1)), n_init = 8, n_iter = 15, seed = 1)
round(cal$par, 2)
}
\references{
Jones DR, Schonlau M, Welch WJ (1998) J Glob Optim 13:455-492
(efficient global optimisation / expected improvement).
}
\seealso{
\code{\link[=gpEmulator]{gpEmulator()}}, \code{\link[=abcSMC]{abcSMC()}}, \code{\link[=personalizeCardio]{personalizeCardio()}}
}
