% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-ukf.R
\name{unscentedKalmanFilter}
\alias{unscentedKalmanFilter}
\title{Unscented Kalman filter (general nonlinear state estimator)}
\usage{
unscentedKalmanFilter(
  f,
  h,
  y,
  Q,
  R,
  x0,
  P0,
  alpha = 0.001,
  beta = 2,
  kappa = 0
)
}
\arguments{
\item{f}{Process model \verb{function(x, k)} returning the next state.}

\item{h}{Measurement model \verb{function(x, k)} returning the predicted measurement.}

\item{y}{Measurement matrix (\verb{n_steps x m}).}

\item{Q}{Process-noise covariance (state x state).}

\item{R}{Measurement-noise covariance (m x m).}

\item{x0, P0}{Initial state mean and covariance.}

\item{alpha, beta, kappa}{Sigma-point spread parameters (defaults 1e-3, 2, 0).}
}
\value{
a \code{ukf_result}: \code{state} (\verb{n_steps x L} filtered means), \code{cov} (final
covariance), \code{innovation} (\verb{n_steps x m}).
}
\description{
A sigma-point (Julier-Uhlmann) Kalman filter for a nonlinear state-space model
\verb{x_k = f(x_\{k-1\}, k) + w}, \code{y_k = h(x_k, k) + v}. Used here to assimilate a
mechanistic twin with sensor data and to estimate augmented parameters, but it
is a stand-alone engine for any such model.
}
\examples{
# track a noisy sine with a random-walk state
set.seed(1); y <- matrix(sin(seq(0, 6, length.out = 60)) + rnorm(60, 0, .1), 60, 1)
fit <- unscentedKalmanFilter(function(x, k) x, function(x, k) x, y,
         Q = matrix(0.05), R = matrix(0.01), x0 = 0, P0 = matrix(1))
fit$state[60]
}
\references{
Julier SJ, Uhlmann JK (1997) SPIE 3068:182-193; Wan & van der Merwe
(2000).
}
\seealso{
\code{\link[=personalizeTwin]{personalizeTwin()}}
}
