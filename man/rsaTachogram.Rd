% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator-respiration.R
\name{rsaTachogram}
\alias{rsaTachogram}
\title{Respiratory sinus arrhythmia tachogram (heart-rate variability generator)}
\usage{
rsaTachogram(
  duration = 300,
  hr0 = 70,
  resp_rate = 15,
  rsa = 0.12,
  mayer = 0.06,
  mayer_freq = 0.1,
  ie_ratio = 0.5,
  noise = 0.01,
  seed = NULL
)
}
\arguments{
\item{duration}{Recording length (s).}

\item{hr0}{Mean heart rate (bpm).}

\item{resp_rate}{Breathing rate (breaths/min); sets the HF peak.}

\item{rsa}{Depth of the respiratory heart-rate modulation (fraction of \code{hr0}).}

\item{mayer}{Depth of the Mayer-wave modulation (fraction of \code{hr0}).}

\item{mayer_freq}{Mayer-wave frequency (Hz).}

\item{ie_ratio}{Inspiration:expiration ratio of the driving respiration.}

\item{noise}{SD of a white heart-rate jitter (fraction of \code{hr0}).}

\item{seed}{Optional RNG seed.}
}
\value{
an \code{rsaTachogram} object: \code{beat_time}, \code{rr} (RR intervals, s), \code{hr}
(instantaneous, bpm), the driving \code{resp} waveform; and the HRV summaries
\code{hf_peak} (Hz of the HF spectral peak), \code{lf_power}, \code{hf_power} and \code{lf_hf}
(the LF/HF ratio).
}
\description{
Generates a heart-rate-variability tachogram by integral pulse frequency
modulation: the instantaneous heart rate is modulated by respiration (faster
on inspiration -- respiratory sinus arrhythmia) and by a ~0.1 Hz Mayer wave,
and a beat is emitted whenever the integral of the instantaneous rate crosses
an integer. The resulting RR-interval series reproduces the high-frequency
(respiratory) and low-frequency (Mayer) peaks of the HRV spectrum.
}
\examples{
hrv <- rsaTachogram(duration = 300, resp_rate = 15, seed = 1)
hrv$hf_peak            # ~ 0.25 Hz, the respiratory rate
}
\references{
Berger RD et al. (1989) IEEE Trans Biomed Eng 36:1049-1055
(IPFM). Task Force (1996) Circulation 93:1043-1065 (HRV bands).
}
\seealso{
\code{\link[=respiration]{respiration()}}, \code{\link[=ecgsyn]{ecgsyn()}}
}
