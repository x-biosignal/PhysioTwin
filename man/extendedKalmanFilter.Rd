% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-filters.R
\name{extendedKalmanFilter}
\alias{extendedKalmanFilter}
\title{Extended Kalman filter (Jacobian-linearised state estimator)}
\usage{
extendedKalmanFilter(f, h, y, Q, R, x0, P0, Fjac = NULL, Hjac = NULL)
}
\arguments{
\item{f, h}{Process and measurement models \verb{function(x, k)}.}

\item{y}{Measurement matrix (\verb{n_steps x m}).}

\item{Q, R}{Process- and measurement-noise covariances.}

\item{x0, P0}{Initial state mean and covariance.}

\item{Fjac, Hjac}{Optional analytic Jacobians \verb{function(x, k)} (rows = outputs);
finite differences are used when \code{NULL}.}
}
\value{
an \code{ekf_result}: \code{state} (\verb{n_steps x L}), \code{cov} (final), \code{innovation}.
}
\description{
A first-order extended Kalman filter for \verb{x_k = f(x_\{k-1\}, k) + w},
\code{y_k = h(x_k, k) + v}. The process and measurement Jacobians are obtained by
central finite differences unless analytic versions are supplied. Same
interface and return shape as \code{\link[=unscentedKalmanFilter]{unscentedKalmanFilter()}}; for a linear-Gaussian
model it reproduces the exact Kalman filter.
}
\examples{
set.seed(1); y <- matrix(cumsum(rnorm(40, 0, .1)) + rnorm(40, 0, .1), 40, 1)
fit <- extendedKalmanFilter(function(x, k) x, function(x, k) x, y,
         Q = matrix(0.05), R = matrix(0.01), x0 = 0, P0 = matrix(1))
fit$state[40]
}
\references{
Jazwinski AH (1970) Stochastic Processes and Filtering Theory.
}
\seealso{
\code{\link[=unscentedKalmanFilter]{unscentedKalmanFilter()}}, \code{\link[=particleFilter]{particleFilter()}}, \code{\link[=ensembleKalmanFilter]{ensembleKalmanFilter()}}
}
