% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-data.R
\name{generateTrainingData}
\alias{generateTrainingData}
\title{Generate a labelled synthetic training set from the twin}
\usage{
generateTrainingData(
  n = 100,
  strength = c(3, 10),
  damping = c(0.1, 0.8),
  stiffness = c(1.5, 4),
  duration = 5,
  dt = 0.02,
  randomize_sensor = TRUE,
  features = TRUE,
  seed = NULL
)
}
\arguments{
\item{n}{Number of virtual subjects.}

\item{strength, damping, stiffness}{Length-2 ranges (uniform) for each parameter.}

\item{duration, dt}{Simulation settings.}

\item{randomize_sensor}{If \code{TRUE}, draw a different IMU noise/bias/scale per
subject (domain randomisation).}

\item{features}{If \code{TRUE} (default) return summary features; if \code{FALSE} return
the raw IMU streams alongside the labels.}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{twin_dataset}: a data frame of features + label columns
(\code{strength}, \code{damping}, \code{stiffness}), or (when \code{features = FALSE}) a list
with \code{labels} and \code{streams}.
}
\description{
Samples \code{n} virtual subjects: physical parameters are drawn from the given
ranges, the IMU sensor is randomised (domain randomisation for robustness),
each twin is simulated and measured, and the sensor features are returned with
the generating parameters as labels. Use it to build ML training data where
the labels are true mechanistic quantities.
}
\examples{
d <- generateTrainingData(20, duration = 4, dt = 0.02, seed = 1)
head(d)
}
\seealso{
\code{\link[=limbTwin]{limbTwin()}}, \code{\link[=imuMeasure]{imuMeasure()}}
}
