% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical-decision.R
\name{evaluateIntervention}
\alias{evaluateIntervention}
\title{Evaluate a delivered intervention against its prediction (close the loop)}
\usage{
evaluateIntervention(
  twin,
  chosen,
  observed_delta,
  readiness = NULL,
  outcome = "rom",
  outcome_sd = 0,
  n_mc = 200,
  duration = 6,
  dt = 0.005,
  theta0 = NULL,
  seed = NULL
)
}
\arguments{
\item{twin}{The personalised \code{movement_twin}.}

\item{chosen}{The delivered \code{twin_intervention}.}

\item{observed_delta}{The measured outcome change at follow-up.}

\item{readiness}{A \code{twin_readiness} (for the parameter posterior).}

\item{outcome, n_mc, duration, dt, theta0, seed}{As in \code{\link[=scoreInterventions]{scoreInterventions()}}.}

\item{outcome_sd}{Outcome observation/session variability (SD). The check uses a
PREDICTION interval combining the parameter (epistemic) uncertainty with this
(aleatoric) noise, so a realistic follow-up is covered -- a parameter-only
interval is over-narrow for a real measurement.}
}
\value{
an \code{intervention_evaluation}: \code{predicted} effect and prediction \code{ci}, the
\code{observed} change, and whether it was \code{within_interval}.
}
\description{
After an intervention is delivered and a follow-up outcome is measured, check
whether the observed change fell within the twin's predicted interval -- the
empirical test that closes the personalise -> predict -> act -> evaluate loop
and calibrates trust in the twin.
}
\examples{
\donttest{
truth <- limbTwin(strength = 6)
imu <- imuMeasure(simulateTwin(truth, 6, 0.01), imuSensor(gyro_noise = 0.03), seed = 1)
fit <- personalizeTwin(imu, limbTwin(strength = 4), estimate = "strength", dt = 0.01)
rdy <- twinReadiness(fit, imu, dt = 0.01)
evaluateIntervention(fit$twin, defaultInterventions()$strengthening, 1.5, rdy,
                     outcome_sd = 0.1, n_mc = 50)
}
}
\seealso{
\code{\link[=scoreInterventions]{scoreInterventions()}}, \code{\link[=clinicalDecision]{clinicalDecision()}}
}
