% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twin-model.R
\name{limbTwin}
\alias{limbTwin}
\title{Build a parameterised movement twin (single-joint limb)}
\usage{
limbTwin(
  inertia = 0.15,
  strength = 5,
  damping = 0.3,
  stiffness = 2.5,
  freq = 1,
  rest = 0,
  mass = 2.5,
  com = 0.2,
  g = 9.81,
  sensor_r = 0.25
)
}
\arguments{
\item{inertia}{Segment moment of inertia about the joint (kg m^2).}

\item{strength}{Active-drive amplitude (N m) -- the muscle strength.}

\item{damping}{Passive viscous damping (N m s / rad).}

\item{stiffness}{Joint stiffness toward \code{rest} (N m / rad).}

\item{freq}{Rhythm of the active drive (Hz).}

\item{rest}{Neutral joint angle (rad).}

\item{mass, com, g}{Segment mass (kg), centre-of-mass distance from the joint
(m) and gravity (m/s^2); set \code{g = 0} for a horizontal-plane movement.}

\item{sensor_r}{Distance of the IMU from the joint along the segment (m), used
by the sensor layer.}
}
\value{
a \code{movement_twin} object (a named parameter list with class).
}
\description{
Constructs a mechanistic single-joint limb model whose parameters map to
physical/clinical quantities (segment inertia, active muscle strength, passive
damping, joint stiffness). It is the object simulated, personalised and
intervened on by the rest of \code{PhysioTwin}.
}
\examples{
tw <- limbTwin(strength = 6, damping = 0.4, stiffness = 3)
sim <- simulateTwin(tw, duration = 4, dt = 0.005)
range(sim$theta)
}
\seealso{
\code{\link[=simulateTwin]{simulateTwin()}}, \code{\link[=personalizeTwin]{personalizeTwin()}}, \code{\link[=insilicoIntervention]{insilicoIntervention()}}
}
