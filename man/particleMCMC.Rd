% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-pmcmc.R
\name{particleMCMC}
\alias{particleMCMC}
\title{Particle marginal Metropolis-Hastings (particle MCMC)}
\usage{
particleMCMC(
  build_model,
  y,
  prior_logd,
  init,
  n_particles = 200,
  n_iter = 3000,
  proposal_sd = 0.1,
  burn_in = 0.3,
  seed = NULL
)
}
\arguments{
\item{build_model}{\verb{function(theta)} returning the state-space model for
parameters \code{theta}: a list \code{f}, \code{h} (\verb{function(x, k)}), \code{Q}, \code{R}, \code{x0}, \code{P0}.}

\item{y}{Observation matrix (\verb{n_steps x m}) or vector.}

\item{prior_logd}{\verb{function(theta)} returning the log prior density (\code{-Inf} off
the support).}

\item{init}{Initial parameter vector (its names label the output).}

\item{n_particles}{Particles in the likelihood-estimating filter.}

\item{n_iter}{MCMC iterations.}

\item{proposal_sd}{Initial per-parameter proposal SD.}

\item{burn_in}{Fraction discarded (and over which the proposal adapts).}

\item{seed}{Optional RNG seed.}
}
\value{
a \code{pmcmc_result}: posterior \code{samples}, \code{acceptance}, \code{mean}, \code{sd} and
2.5/50/97.5\% \code{quantiles}.
}
\description{
Samples the parameter posterior of a state-space model from a WHOLE observation
series, when the likelihood is intractable. Each proposed parameter is scored by
a bootstrap particle filter's unbiased estimate of the marginal likelihood
\verb{p(y_\{1:T\} | theta)}, which enters the Metropolis-Hastings acceptance ratio;
the resulting chain targets the exact parameter posterior (Andrieu-Doucet-
Holenstein). The proposal covariance adapts during burn-in.
}
\examples{
\donttest{
# recover the AR coefficient of a linear-Gaussian state-space model from the series
set.seed(1); a <- 0.7; x <- numeric(60); for (k in 2:60) x[k] <- a * x[k-1] + rnorm(1, 0, 0.3)
y <- matrix(x + rnorm(60, 0, 0.3), 60, 1)
bm <- function(th) list(f = function(x, k) th[1] * x, h = function(x, k) x,
                        Q = matrix(0.09), R = matrix(0.09), x0 = 0, P0 = matrix(1))
fit <- particleMCMC(bm, y, function(th) dunif(th[1], -1, 1, log = TRUE),
                    init = c(a = 0.4), n_particles = 100, n_iter = 800, seed = 1)
fit$mean
}
}
\references{
Andrieu C, Doucet A, Holenstein R (2010) J R Stat Soc B 72:269-342.
}
\seealso{
\code{\link[=particleFilter]{particleFilter()}}, \code{\link[=metropolis]{metropolis()}}, \code{\link[=personalizeCardio]{personalizeCardio()}}
}
