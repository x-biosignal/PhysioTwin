% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assimilate-mcmc.R
\name{metropolis}
\alias{metropolis}
\title{Adaptive random-walk Metropolis-Hastings sampler}
\usage{
metropolis(
  logpost,
  init,
  n_iter = 6000,
  proposal_sd = 0.5,
  burn_in = 0.4,
  seed = NULL
)
}
\arguments{
\item{logpost}{\verb{function(theta)} returning the log-posterior (up to a constant);
may return \code{-Inf} outside the support.}

\item{init}{Initial parameter vector (its names label the output).}

\item{n_iter}{Number of iterations.}

\item{proposal_sd}{Initial per-parameter proposal SD.}

\item{burn_in}{Fraction discarded (and over which the proposal adapts).}

\item{seed}{Optional RNG seed.}
}
\value{
an \code{mcmc_result}: posterior \code{samples}, \code{acceptance} rate, \code{mean}, \code{sd},
and 2.5/50/97.5\% \code{quantiles}.
}
\description{
Samples from a log-posterior by random-walk Metropolis-Hastings. The Gaussian
proposal covariance adapts to the empirical covariance of the chain during the
burn-in (Haario et al. 2001), then is frozen, so correlated targets are sampled
efficiently while the stationary distribution is preserved.
}
\examples{
# posterior of a normal mean: 20 obs, N(0,10) prior
set.seed(1); y <- rnorm(20, 2, 1)
lp <- function(m) sum(dnorm(y, m, 1, log = TRUE)) + dnorm(m, 0, 10, log = TRUE)
fit <- metropolis(lp, init = c(mu = 0), n_iter = 4000, seed = 1)
fit$mean
}
\references{
Metropolis et al. (1953); Haario H, Saksman E, Tamminen J (2001)
Bernoulli 7:223-242 (adaptive Metropolis).
}
\seealso{
\code{\link[=abcSMC]{abcSMC()}}, \code{\link[=abcCalibration]{abcCalibration()}}, \code{\link[=gpCalibrate]{gpCalibrate()}}
}
