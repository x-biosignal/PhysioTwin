% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator-cardiovascular.R
\name{windkessel}
\alias{windkessel}
\title{Windkessel arterial blood-pressure generator}
\usage{
windkessel(
  duration = 10,
  hr = 70,
  stroke_volume = 80,
  R = 1,
  C = 1.9,
  Zc = 0.05,
  systolic_frac = 0.3,
  elements = 3,
  sfp = 250,
  p0 = 80
)
}
\arguments{
\item{duration}{Recording length (s).}

\item{hr}{Heart rate (bpm).}

\item{stroke_volume}{Stroke volume ejected each beat (mL).}

\item{R}{Peripheral (systemic vascular) resistance (mmHg*s/mL).}

\item{C}{Arterial compliance (mL/mmHg).}

\item{Zc}{Proximal characteristic impedance (mmHg*s/mL); used when \code{elements = 3}.}

\item{systolic_frac}{Fraction of the cardiac cycle occupied by ejection.}

\item{elements}{2 or 3 (2-element = pure RC; 3-element adds \code{Zc}).}

\item{sfp}{Output sampling rate (Hz).}

\item{p0}{Initial compliance-node pressure (mmHg).}
}
\value{
a \code{windkessel} object: \code{time}, \code{pressure} (arterial pressure, mmHg),
\code{flow} (aortic inflow, mL/s), and the derived \code{systolic}, \code{diastolic}, \code{map}
(mean arterial pressure) and \code{pp} (pulse pressure), measured over the last
settled beats.
}
\description{
Simulates arterial pressure with the classic Windkessel model: a half-sine
ventricular ejection each beat charges the arterial compliance \code{C}, which
drains through the peripheral resistance \code{R}; the 3-element variant adds a
proximal characteristic impedance \code{Zc}. During diastole (no inflow) the
pressure decays exponentially with time constant \code{R * C}. With physiological
parameters it reproduces the systolic/diastolic/mean/pulse pressures and the
diastolic run-off.
}
\examples{
bp <- windkessel(duration = 10, hr = 70)
round(c(bp$systolic, bp$diastolic))     # ~ 120 / 80 mmHg
}
\references{
Westerhof N, Lankhaar J-W, Westerhof BE (2009) The arterial
Windkessel. Med Biol Eng Comput 47:131-141.
}
\seealso{
\code{\link[=baroreflex]{baroreflex()}}, \code{\link[=ecgsyn]{ecgsyn()}}, \code{\link[=respiration]{respiration()}}
}
