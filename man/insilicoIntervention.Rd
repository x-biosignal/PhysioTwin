% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervention.R
\name{insilicoIntervention}
\alias{insilicoIntervention}
\title{Predict the effect of an in-silico intervention}
\usage{
insilicoIntervention(
  twin,
  scale = list(),
  set = list(),
  duration = 6,
  dt = 0.005,
  theta0 = 0.1,
  omega0 = 0
)
}
\arguments{
\item{twin}{A \code{movement_twin} (typically a personalised one).}

\item{scale}{Named list of MULTIPLICATIVE changes, e.g. \code{list(strength = 1.3, stiffness = 0.7)}.}

\item{set}{Named list of ABSOLUTE parameter values to set.}

\item{duration, dt, theta0, omega0}{Simulation settings (as \code{\link[=simulateTwin]{simulateTwin()}}).}
}
\value{
an \code{insilico_intervention}: \code{before}, \code{after} (trajectories),
\code{outcomes} (before/after/relative-change table) and the modified \code{twin}.
}
\description{
Applies parameter changes to a (personalised) twin -- multiplicatively via
\code{scale} and/or by absolute values via \code{set} -- re-simulates, and reports how
the movement outcomes change. This is the digital twin used as an experiment:
"if this subject's muscle were 30\% stronger, how would the movement change?".
}
\examples{
tw <- limbTwin(strength = 5, damping = 0.6, stiffness = 3)
iv <- insilicoIntervention(tw, scale = list(strength = 1.3, damping = 0.7))
iv$outcomes
}
\seealso{
\code{\link[=personalizeTwin]{personalizeTwin()}}, \code{\link[=simulateTwin]{simulateTwin()}}
}
