% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment-design.R
\name{optimalDesign}
\alias{optimalDesign}
\title{Bayesian optimal experimental design for twin identification}
\usage{
optimalDesign(
  twin,
  estimate = "strength",
  designs,
  design_var = "freq",
  sensor = imuSensor(),
  duration = 6,
  dt = 0.01,
  theta0 = 0.1,
  criterion = c("D", "A")
)
}
\arguments{
\item{twin}{A \code{movement_twin} giving the assumed (prior) parameter values.}

\item{estimate}{Parameter(s) whose identifiability is optimised.}

\item{designs}{Numeric vector of candidate values for the design variable.}

\item{design_var}{The twin field the design varies (e.g. \code{"freq"}, \code{"duration"}).}

\item{sensor}{An \code{imuSensor} (its \code{gyro_noise}/\code{accel_noise} weight the channels).}

\item{duration, dt, theta0}{Simulation settings for the observable.}

\item{criterion}{\code{"D"} (max log-det Fisher information) or \code{"A"} (min total CRLB variance).}
}
\value{
an \code{optimal_design}: a \code{table} (\code{design}, \code{logdet_fim}, \code{score}, and a
\verb{crlb_<param>} Cramer-Rao SD per estimated parameter), the \code{best} design, and
the \code{estimate}/\code{design_var}/\code{criterion}.
}
\description{
Chooses the experimental condition that will best identify a movement twin's
parameters. For each candidate value of a design variable (e.g. the driving
frequency), it forms the Fisher information matrix of the noise-weighted IMU
observable about the parameters and scores the design by an optimality criterion
-- D-optimality (the log-determinant of the information, maximising overall
information) or A-optimality (minimising the total Cramer-Rao variance). The most
informative condition is the experiment worth running to pin the parameters down.
}
\examples{
od <- optimalDesign(limbTwin(), estimate = "strength",
                    designs = seq(0.4, 2.4, by = 0.2), design_var = "freq",
                    duration = 5, dt = 0.02)
od$best        # the most informative driving frequency
}
\references{
Chaloner K, Verdinelli I (1995) Bayesian experimental design: a
review. Statist Sci 10:273-304; Fedorov (1972) D-optimality.
}
\seealso{
\code{\link[=personalizeTwin]{personalizeTwin()}}, \code{\link[=validateTwin]{validateTwin()}}, \code{\link[=profileLikelihood]{profileLikelihood()}}
}
