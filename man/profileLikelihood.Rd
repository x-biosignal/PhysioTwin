% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identifiability-sensitivity.R
\name{profileLikelihood}
\alias{profileLikelihood}
\title{Profile-likelihood practical identifiability}
\usage{
profileLikelihood(
  imu,
  prior,
  param,
  over = c(0.4, 2),
  nuisance = character(0),
  n_grid = 15L,
  dt = 0.02,
  gyro_var = 0.03^2,
  accel_var = 0.12^2
)
}
\arguments{
\item{imu}{An IMU recording (\verb{gyro, ax, ay}), real or simulated.}

\item{prior}{A \code{movement_twin} giving the fixed parameters and start point.}

\item{param}{The parameter to profile.}

\item{over}{Length-2 multiplicative range around \code{prior[[param]]} (default
\code{c(0.4, 2.0)}), or an explicit numeric grid.}

\item{nuisance}{Other parameters re-optimised at each grid point.}

\item{n_grid}{Grid size (default 15).}

\item{dt, gyro_var, accel_var}{Sample period and measurement variances.}
}
\value{
a \code{profile_likelihood}: the \code{profile} (grid value, cost), the cost
\code{minimum}, the 95\% \code{ci} (or \code{NA} if open), and \code{identifiable} (logical).
}
\description{
Assesses whether a parameter is \emph{practically} identifiable from a recording:
it is fixed across a grid of values and the remaining \code{nuisance} parameters
(plus a start angle and gyro bias) are re-optimised, tracing the fit cost. A
clear minimum bounded by the chi-square threshold gives a confidence interval;
a flat profile means the parameter cannot be pinned down.
}
\examples{
\donttest{
tw <- limbTwin(strength = 6, damping = 0.5, stiffness = 6)
imu <- imuMeasure(simulateTwin(tw, 8, 0.02, theta0 = 0.2),
                  imuSensor(gyro_noise = 0.03, accel_noise = 0.12), seed = 1)
profileLikelihood(imu, tw, "strength", nuisance = "damping", n_grid = 9)$ci
}
}
\references{
Raue A, et al. (2009) Bioinformatics 25:1923-1929.
}
\seealso{
\code{\link[=validateTwin]{validateTwin()}}, \code{\link[=sobolIndices]{sobolIndices()}}
}
