% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator-ecg.R
\name{ecgsyn}
\alias{ecgsyn}
\title{ECGSYN mechanistic ECG generator (McSharry dynamical model)}
\usage{
ecgsyn(
  duration = 10,
  hr = 60,
  sfecg = 256,
  hrv_sd = 0.04,
  lfhfratio = 0.5,
  anoise = 0,
  scale_mv = c(-0.4, 1.2),
  theta = c(-60, -15, 0, 15, 90) * pi/180,
  a = c(1.2, -5, 30, -7.5, 0.75),
  b = c(0.25, 0.1, 0.1, 0.1, 0.4),
  seed = NULL
)
}
\arguments{
\item{duration}{Length (s).}

\item{hr}{Mean heart rate (bpm).}

\item{sfecg}{Sampling frequency (Hz).}

\item{hrv_sd}{RR-interval standard deviation (s) -- the amount of HRV (0 = fixed
rate).}

\item{lfhfratio}{LF/HF power ratio of the RR spectrum (autonomic balance).}

\item{anoise}{Additive measurement-noise SD (mV).}

\item{scale_mv}{Length-2 physiological output range \code{c(min, max)} in mV that the
raw model output is affinely mapped onto (McSharry default \code{c(-0.4, 1.2)});
\code{NULL} keeps the raw model units.}

\item{theta, a, b}{P,Q,R,S,T event angles (rad), amplitudes and widths (McSharry
defaults).}

\item{seed}{Optional RNG seed.}
}
\value{
an \code{ecgsyn} object: \code{time}, \code{ecg}, the \code{rr} intervals, \code{beats} (R-peak
times), \code{sfecg}.
}
\description{
Generates a realistic ECG from the ECGSYN 3-state limit-cycle model: P/Q/R/S/T
waves arise as the trajectory passes Gaussian events, and heart-rate
variability is injected via a bimodal RR tachogram. The output has physiological
morphology AND HRV.
}
\examples{
e <- ecgsyn(duration = 6, hr = 60, sfecg = 256, seed = 1)
range(e$ecg)          # P/Q/R/S/T morphology
length(e$beats)       # ~ 6 beats
}
\references{
McSharry PE, Clifford GD, Tarassenko L, Smith LA (2003) IEEE Trans
Biomed Eng 50:289-294.
}
\seealso{
\code{\link[=jansenRit]{jansenRit()}}, \code{\link[=windkessel]{windkessel()}}, \code{\link[=rsaTachogram]{rsaTachogram()}}, \code{\link[=kuramoto]{kuramoto()}}, \code{\link[=validateTwin]{validateTwin()}}
}
