% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical-decision.R
\name{reconcileEvidence}
\alias{reconcileEvidence}
\title{Reconcile a mechanistic prediction with external evidence}
\usage{
reconcileEvidence(mech_est, mech_se, ev_est, ev_se)
}
\arguments{
\item{mech_est, mech_se}{Mechanistic effect estimate and its SE.}

\item{ev_est, ev_se}{Evidence-based effect estimate and its SE.}
}
\value{
an \code{evidence_reconciliation}: combined \code{estimate}/\code{se}, the discrepancy
\code{z}, and \code{agreement} (\code{agree}/\code{disagree}, at |z| < 2).
}
\description{
Combines the twin's mechanistic effect estimate with an external (e.g.
literature) estimate by inverse-variance (precision) weighting -- the Bayesian
conjugate-normal update -- and flags whether the two AGREE. Evidence complements
the mechanistic prediction; it is never overridden.
}
\examples{
reconcileEvidence(mech_est = 0.30, mech_se = 0.05, ev_est = 0.26, ev_se = 0.04)
}
\seealso{
\code{\link[=scoreInterventions]{scoreInterventions()}}, \code{\link[=abcCalibration]{abcCalibration()}}
}
